<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.admin.model.AdminDAO">

	<select id="adminlogin"
		parameterType="com.smhrd.admin.model.AdminDTO"
		resultType="com.smhrd.admin.model.AdminDTO">
		SELECT USER_ID, USER_PHONE
		FROM USERS
		WHERE USER_ID =
		#{user_id}
		AND USER_PW = #{user_pw}
	</select>

	<select id="selectAll"
		resultType="com.smhrd.admin.model.AdminDTO">
		SELECT USER_ID, USER_PHONE, USER_NAME, USER_EMAIL,
		USER_ADDR, USER_GRADE, JOINED_AT FROM USERS
	</select>

	<select id="searchUserById" parameterType="java.lang.String" resultType="com.smhrd.admin.model.AdminDTO">
    SELECT USER_ID, USER_PW, USER_PHONE, USER_NAME, USER_EMAIL, USER_ADDR, USER_GRADE, JOINED_AT
    FROM USERS
    WHERE USER_ID = #{userId}
</select>

</mapper>